# Simplified Choreo Configuration for React Frontend
# Choreo will auto-detect React/Vite projects

schemaVersion: 1.0

# Build configuration
build:
  type: buildpack
  buildpack: nodejs
  nodeVersion: "18"
  
# Note: Choreo will auto-detect:
# - package.json in the component directory
# - Build command: npm run build
# - Output directory: dist (from vite.config.js)
