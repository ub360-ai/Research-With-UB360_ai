# ============= Google Gemini API =============
# Get your FREE API key at: https://aistudio.google.com/
GOOGLE_API_KEY=your_google_gemini_api_key_here

# Optional: Customize Gemini model (default: gemini-2.0-flash)
# Recommended models:
#   - gemini-2.0-flash (RECOMMENDED: Latest stable, good free tier)
#   - gemini-1.5-flash (Alternative: Fast, generous free tier)
#   - gemini-1.5-pro (More capable, lower rate limits)
# GEMINI_MODEL=gemini-2.0-flash
# GEMINI_TEMPERATURE=0.1

# ============= Vector Database =============
# ChromaDB storage location (default: ./chroma_db)
# CHROMA_PERSIST_DIR=./chroma_db
# CHROMA_COLLECTION_NAME=research_documents

# ============= Embedding Model =============
# Sentence transformer model for embeddings
# EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# ============= Document Processing =============
# Maximum file size in MB (default: 50)
# MAX_FILE_SIZE_MB=50

# Text chunking parameters
# CHUNK_SIZE=1000
# CHUNK_OVERLAP=200

# Upload directory (default: ./uploads)
# UPLOAD_DIR=./uploads

# ============= Search Configuration =============
# Number of search results to return by default
# DEFAULT_SEARCH_RESULTS=5
# MAX_SEARCH_RESULTS=20

# ============= Rate Limiting =============
# Requests per minute (default: 60)
# RATE_LIMIT_PER_MINUTE=60

# ============================================
# IMPORTANT: Never commit this file to Git!
# Add .env to your .gitignore file
# ============================================
